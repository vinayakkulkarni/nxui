{
  "$schema": "https://shadcn-vue.com/schema/registry-item.json",
  "name": "paper-metaballs",
  "type": "registry:ui",
  "title": "Metaballs",
  "description": "Up to 20 gooey blobs moving around the center and merging into smooth organic shapes. Ported from Paper Shaders (Apache-2.0).",
  "dependencies": ["@paper-design/shaders"],
  "registryDependencies": ["https://nxui.geoql.in/r/paper-shader-mount.json"],
  "files": [
    {
      "path": "registry/new-york/paper-metaballs/PaperMetaballs.vue",
      "content": "<script setup lang=\"ts\">\n  // Vue wrapper for the Paper Shaders metaballs\n  // (https://github.com/paper-design/shaders, Apache-2.0, © Lost Coast Labs).\n  // Mirrors @paper-design/shaders-react's <Metaballs> defaultPreset.\n  import { computed } from 'vue';\n  import {\n    metaballsFragmentShader,\n    getShaderColorFromString,\n    getShaderNoiseTexture,\n    ShaderFitOptions,\n  } from '@paper-design/shaders';\n  import PaperShaderMount from '../paper-shader-mount/PaperShaderMount.vue';\n  import type { PaperMetaballsProps } from './types';\n\n  const props = withDefaults(defineProps<PaperMetaballsProps>(), {\n    colorBack: '#000000',\n    colors: () => ['#6e33cc', '#ff5500', '#ffc105', '#ffc800', '#f585ff'],\n    count: 10,\n    size: 0.83,\n    scale: 1,\n    speed: 1,\n    frame: 0,\n    fit: 'contain',\n    rotation: 0,\n    originX: 0.5,\n    originY: 0.5,\n    offsetX: 0,\n    offsetY: 0,\n    worldWidth: 0,\n    worldHeight: 0,\n    class: '',\n  });\n\n  const uniforms = computed(() => ({\n    u_colorBack: getShaderColorFromString(props.colorBack),\n    u_colors: props.colors.map(getShaderColorFromString),\n    u_colorsCount: props.colors.length,\n    u_size: props.size,\n    u_count: props.count,\n    u_noiseTexture: getShaderNoiseTexture(),\n    u_fit: ShaderFitOptions[props.fit],\n    u_rotation: props.rotation,\n    u_scale: props.scale,\n    u_offsetX: props.offsetX,\n    u_offsetY: props.offsetY,\n    u_originX: props.originX,\n    u_originY: props.originY,\n    u_worldWidth: props.worldWidth,\n    u_worldHeight: props.worldHeight,\n  }));\n</script>\n\n<template>\n  <PaperShaderMount\n    :fragment-shader=\"metaballsFragmentShader\"\n    :uniforms=\"uniforms\"\n    :speed=\"props.speed\"\n    :frame=\"props.frame\"\n    :class=\"props.class\"\n  />\n</template>\n",
      "type": "registry:ui",
      "target": "components/ui/paper-metaballs/PaperMetaballs.vue"
    },
    {
      "path": "registry/new-york/paper-metaballs/index.ts",
      "content": "export { default as PaperMetaballs } from './PaperMetaballs.vue';\n",
      "type": "registry:ui",
      "target": "components/ui/paper-metaballs/index.ts"
    },
    {
      "path": "registry/new-york/paper-metaballs/types.ts",
      "content": "export type PaperMetaballsFit = 'none' | 'contain' | 'cover';\n\nexport interface PaperMetaballsProps {\n  /** Background color behind the balls. */\n  colorBack?: string;\n  /** Up to 8 base colors cycled across the balls. */\n  colors?: string[];\n  /** Number of balls (1 to 20). */\n  count?: number;\n  /** Size of the balls (0 to 1). */\n  size?: number;\n  /** Animation speed multiplier. 0 renders a static frame. */\n  speed?: number;\n  /** Initial/seek frame for deterministic rendering. */\n  frame?: number;\n  /** How the graphic fits the canvas. */\n  fit?: PaperMetaballsFit;\n  /** Overall zoom level (0.01 to 4). */\n  scale?: number;\n  /** Overall rotation angle in degrees (0 to 360). */\n  rotation?: number;\n  /** Horizontal origin for world positioning (0 to 1). */\n  originX?: number;\n  /** Vertical origin for world positioning (0 to 1). */\n  originY?: number;\n  /** Horizontal offset of the graphics center (-1 to 1). */\n  offsetX?: number;\n  /** Vertical offset of the graphics center (-1 to 1). */\n  offsetY?: number;\n  /** Virtual world width before canvas fitting. */\n  worldWidth?: number;\n  /** Virtual world height before canvas fitting. */\n  worldHeight?: number;\n  /** Additional CSS classes. */\n  class?: string;\n}\n",
      "type": "registry:ui",
      "target": "components/ui/paper-metaballs/types.ts"
    }
  ]
}
