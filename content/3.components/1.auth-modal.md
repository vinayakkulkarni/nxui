---
title: Auth Modal
description: Vue port of the auth-modal component from componentry.fun.
---

::demo-auth-modal
::

## Installation

```bash
npx shadcn-vue@latest add "https://nxui.dev/r/auth-modal.json"
```

## Usage

```vue
<script setup lang="ts">
  import { ref } from 'vue';
  import AuthModal from '~/components/ui/AuthModal.vue';

  const open = ref(false);
</script>

<template>
  <button @click="open = true">
    Open Auth Modal
  </button>
  <AuthModal v-model:open="open" />
</template>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | `false` | Controls modal visibility via v-model |
| `class` | `string` | `''` | Additional CSS classes for styling |
